apiVersion: v1
kind: Service
metadata:
  name: trading-api
  namespace: trading-platform
  labels:
    app: trading-api
    component: api
spec:
  type: ClusterIP
  selector:
    app: trading-api
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
