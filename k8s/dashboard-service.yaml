apiVersion: v1
kind: Service
metadata:
  name: trading-dashboard
  namespace: trading-platform
  labels:
    app: trading-dashboard
    component: dashboard
spec:
  type: ClusterIP
  selector:
    app: trading-dashboard
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
